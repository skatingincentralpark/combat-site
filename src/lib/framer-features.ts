/*
 * If you're using a bundler like Webpack or Rollup, we can pass a dynamic import
 * function to features that will fetch features only after we've performed our initial render.
 * https://www.framer.com/docs/guide-reduce-bundle-size/
 */
import { domMax } from "framer-motion";
export default domMax;
